[project]
name = "llama-stack-provider-grumpy-guard"
version = "0.1.0"
description = "Grumpy Guard safety provider for llama stack"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "llama-stack",
    "pydantic",
]
